# XcodeGen output (regenerate with `xcodegen generate` or `./build.sh`)
*.xcodeproj

# Build artifacts
.build/
dist/
DerivedData/

# Local signing config (Team ID / identity) — keep .env.example tracked
.env

# macOS cruft
.DS_Store
